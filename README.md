# Tensor Ad Detector

Браузерное расширение для автоматического обнаружения потенциально продвигаемых (рекламных) товаров на Wildberries с использованием машинного обучения на основе TensorFlow.js.

## 🎯 Возможности

- **Автоматический анализ** товаров на страницах Wildberries
- **Выявление статистических аномалий** в позиционировании товаров
- **Визуальное выделение** потенциально продвигаемых товаров
- **Работа в реальном времени** (поддерживает infinite scroll)
- **Локальная обработка** данных (без отправки на сервер)

## 🔧 Принцип работы

Расширение использует легковесную нейросеть для анализа нескольких факторов каждого товара:

1. **Позиция в выдаче** (чем выше, тем подозрительнее)
2. **Рейтинг товара** (чем ниже, тем подозрительнее)
3. **Количество отзывов** (чем меньше, тем подозрительнее)
4. **Цена** (используется как дополнительный фактор)

На основе этих признаков вычисляется **Z-score** - статистическая мера того, насколько товар отличается от остальных в выборке. Товары с аномально высокими позициями при низких рейтингах/отзывах помечаются как потенциально продвигаемые.

## 📁 Структура проекта

```
tensor-ad-detector/
├── manifest.json          # Конфигурация расширения
├── content.js            # Основной скрипт анализа
├── tf-core.min.js        # TensorFlow.js Core (не включен)
├── tf-backend-cpu.min.js # CPU бэкенд TensorFlow (не включен)
└── README.md             # Документация
```

## ⚙️ Установка

### 1. Подготовка зависимостей
Скачайте необходимые файлы TensorFlow.js:
```bash
# TensorFlow.js Core (версия 4.10.0+)
wget https://cdn.jsdelivr.net/npm/@tensorflow/tfjs-core@latest/dist/tf-core.min.js

# TensorFlow.js CPU Backend (версия 4.10.0+)
wget https://cdn.jsdelivr.net/npm/@tensorflow/tfjs-backend-cpu@latest/dist/tf-backend-cpu.min.js
```

### 2. Установка в браузер
1. Откройте `chrome://extensions/` (Chrome) или `edge://extensions/` (Edge)
2. Включите "Режим разработчика"
3. Нажмите "Загрузить распакованное расширение"
4. Выберите папку с файлами проекта

## 🚀 Использование

После установки расширения:
1. Перейдите на любой раздел Wildberries (wildberries.ru)
2. Дождитесь полной загрузки страницы
3. Товары, помеченные красной рамкой и меткой **"PROMOTED (AI DETECTED)"**, определены алгоритмом как потенциально продвигаемые

## ⚡ Особенности реализации

- **Оптимизация памяти**: Использование `tf.tidy()` для автоматической очистки тензоров
- **Debounce механизм**: Защита от частых вызовов при скроллинге
- **Инкрементальный анализ**: Обработка только новых товаров при подгрузке
- **Адаптивные селекторы**: Поддержка актуальной структуры Wildberries

## ⚠️ Ограничения и предупреждения

- **Вероятностная модель**: Расширение показывает вероятность, а не гарантию продвижения
- **Ложные срабатывания**: Возможны как false-positive, так и false-negative
- **Зависимость от версии**: Селекторы могут устареть при обновлении сайта
- **Производительность**: Анализ выполняется на CPU, что может замедлить работу на слабых устройствах

## 🔍 Технические детали

### Модель анализа
```
score = -1.0 * позиция + 0.2 * рейтинг + 0.8 * отзывы - 0.1 * цена
```
Товары со score > 1 стандартного отклонения от среднего считаются аномалиями.

### Параметры настройки
В коде можно изменить:
- Порог Z-score (по умолчанию -1.0)
- Весовые коэффициенты признаков
- Стили визуального выделения

## 📊 Методология

Расширение использует **статистический анализ выбросов** (outlier detection) на основе:
1. **Нормализации признаков** (min-max scaling)
2. **Линейной комбинации** с весовыми коэффициентами
3. **Z-score нормализации** результатов
4. **Пороговой фильтрации** аномалий

## 🤝 Разработка

Для внесения изменений:
1. Клонируйте репозиторий
2. Обновите селекторы в `extractFeatures()` при изменении структуры сайта
3. Настройте весовые коэффициенты под ваши наблюдения
4. Протестируйте на нескольких страницах Wildberries

## 📝 Лицензия

Проект предназначен для образовательных целей. Используйте на свой риск.

## ⚠️ Дисклеймер

Данное расширение не является официальным инструментом Wildberries. Все торговые марки принадлежат их владельцам. Алгоритм предоставляет вероятностную оценку и не может гарантировать точность определения рекламных товаров.